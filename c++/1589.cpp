#include<bits/stdc++.h>

using namespace std;

int main() {
    int casos;
    cin>>casos;
    while(casos--) {
        long long int a, b;
        cin>>a>>b;
        printf("%lld\n", (a + b));
    }
    return 0;
}