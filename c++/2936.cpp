#include <bits/stdc++.h>
 
using namespace std;
 
int main() {
    long long int a, b, c, d, e;
    cin>>a>>b>>c>>d>>e;
    long long int res = (a * 300) + (b * 1500) + (c * 600) + (d * 1000) + (e * 150) + 225;
    cout<<res<<endl;
    return 0;
}