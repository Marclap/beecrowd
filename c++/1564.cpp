#include <bits/stdc++.h>

using namespace std;

int main(){
    int casos;
    while(scanf("%d", &casos) != EOF) {
        if(!casos)
            printf("vai ter copa!\n");
        else
            printf("vai ter duas!\n");
    }
    return 0;
}